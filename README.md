# spring-crud-api

Simple Spring Boot 3 + Java 21 REST API (CRUD example)

### Features
- Create, read, update, delete posts
- Validation + proper error messages
- H2 database (in-memory)
- Auto timestamps
- Clean JSON responses

### Run
```bash
./mvnw spring-boot:run
